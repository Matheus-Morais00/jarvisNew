name: JARVIS Android Build

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

env:
  # O pubspec.yaml exige Dart SDK >=3.13.2 (environment: sdk: ^3.13.2).
  # O Flutter 3.35.3 traz o Dart 3.9.2, que é MAIS ANTIGO que o exigido,
  # e por isso o "flutter pub get" falhava com erro de version solving.
  # O Flutter 3.47.2 traz o Dart 3.13.x, que satisfaz a exigência.
  FLUTTER_VERSION: '3.47.2'

jobs:
  build-android:
    name: Analyze, test and build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Install dependencies
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze

      - name: Unit and widget tests
        run: flutter test

      - name: Build release APK
        run: flutter build apk --release

      - name: Rename APK
        run: cp build/app/outputs/flutter-apk/app-release.apk JARVIS-${{ github.run_number }}.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-apk-${{ github.run_number }}
          path: JARVIS-${{ github.run_number }}.apk
          if-no-files-found: error
          retention-days: 30
